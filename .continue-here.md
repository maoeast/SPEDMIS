# Current State
- 任务：SPEDMIS 原生 Electron AI 教师工作台 Phase 2「知识与智能体治理」已完成（未提交）。
- 已完成：
  - 迁移 7 个知识技能 + 14 份引用（内联为 `modules/ai-skill-catalog.json` 数据模块，非 `.md`，因 `build.files` 排除 `!**/*.md`）。
  - 按绑定精确注入（三态 referenceIds）、字符/技能/引用数硬上限 + 上下文 token 感知（有效上限 80000 字符）+ 结构化截断标记。
  - 每条 assistant 消息落库「知识快照 + provenance」（`ai_message.knowledge_snapshot/knowledge_provenance`）。
  - schema v2→v3 迁移；内置技能按 `code + content_version` 版本门控幂等升级（不覆盖自定义）；内置绑定 seed 用 `ON CONFLICT DO NOTHING` 保留用户微调。
  - 智能体治理：内置可启停不可删；自定义智能体增删改 + 技能绑定；内置「重置绑定」；删除自定义级联绑定、有会话时拒绝（`agent_in_use`）、删除前 abort 在途请求。
- 产出物（新）：`modules/ai-skill-catalog.{js,json}`、`modules/ai-agent-skill-bindings.js`、`modules/ai-knowledge-injector.js`、`tools/gen-skill-catalog.cjs`（本地生成器，gitignore）、`test/ai-skill-catalog.test.js`、`test/ai-knowledge-injector.test.js`、`test/ai-agent-governance.test.js`。
- 产出物（改）：`modules/ai-database.js`（schema v3 等）、`modules/ai-service.js`（注入钩子 + 治理方法）、`modules/ai-ipc.js`+`ai-preload.js`（+11 通道）、`ai-assistant.{html,css,js}`（知识目录/徽章/治理面板/绑定编辑器）、`.gitignore`（`test/*`+`!test/ai-*.test.js`）。
- 验证：AI 套件 63/63 通过（基线 32 → +31）；`npm run build` 成功，`ai-skill-catalog.json` 随包、无 `.md` 出货；`git diff --check` 干净。

# Next Action
- 等待用户指示：人工验收（§7：7 技能/14 引用可追溯、绑定可控、上下文有界、自定义不影响内置升级）或提交（计划 §11：仅在用户明确要求时 commit/push）。当前改动均未提交。

# Blockers
- 全量 Jest 仍有 4 个基线失败套件（`usage-stats`/`module-name-manager`/`entry-module-manager`/`permission-manager`），固定写 `C:\Users\test`，当前账号无权限；与本阶段无关，AI 回归不受影响。
- 真实 Provider 与安装包的人工验收仍未由用户确认（自动测试 + 构建已绿）。
- `PROJECT_CONTEXT.md` 含旧混合编码，继续只追加不重写。

# Key Decisions
- 保持原生 HTML、CommonJS Electron Main/Preload、sql.js，不引入 Vue/Pinia/Element Plus 或额外原生依赖。
- 技能内容以 JSON 数据模块内联（`ai-skill-catalog.json`，薄 JS 包装冻结/深拷贝），用本地生成器从源盘逐字读取，保证零失真 + 转义安全；不随包发 `.md`。
- 用 `content_version` 做幂等升级门控（源 loader 丢弃 version）；新增结构化截断标记 + prompt 快照/provenance（源均缺失）。
- 绑定 seed `DO NOTHING` 保留用户微调，治理层「重置内置绑定」恢复 seed。
- `.gitignore` 由 `test/` 改为 `test/*` + `!test/ai-*.test.js`（git 规则：父目录整体忽略时取反无效）。

# Relevant Files
- .continue-here.md
- PROJECT_CONTEXT.md
- docs/planning/2026-07-23-spedmis-ai-agent-chat-migration-plan.md（§7 Phase 2 范围、§11 门禁）
