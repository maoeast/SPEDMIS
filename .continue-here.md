# Current State
- 任务：将首页默认互斥入口切换为“综合测评领域”，并为 IEP 页面提供右下角悬浮的返回 / 刷新 / 前进按钮。
- 已完成：`config.js`、`index.html`、`advanced-settings.html` 将默认 `selectedModule` 统一改为 `iep`；`main.js` 为 IEP 窗口启用 `webviewTag`；`iep/index.html` 改为 Electron 壳页并内嵌 `iep/embedded-entry.html`；顶部工具栏已移除，仅保留右下角悬浮 FA 图标按钮；相关 Jest 回归已通过。
- 产出物：`config.js`、`index.html`、`advanced-settings.html`、`main.js`、`iep/index.html`、`iep/embedded-entry.html`、`iep/config.json`、`iep/src/**`、`test/entry-module-manager.test.js`、`test/activation-status.test.js`、`test/iep-shell.test.js`

# Next Action
- 运行 `npm start`，在真实 Electron 窗口中手动确认右下角悬浮按钮的位置与遮挡情况，必要时只微调 `iep/index.html` 中 `.shell-actions` 的 `right` / `bottom` 值。

# Blockers
- None

# Key Decisions
- 默认入口使用 `selectedModule = 'iep'`：只影响未配置过的机器，已持久化的 `psy` 选择不自动迁移。
- IEP 入口采用壳页结构：`iep/index.html` 负责悬浮导航，`iep/embedded-entry.html` 保留原业务页内容。
- 顶部工具栏已删除：仅保留右下角悬浮按钮，减少对业务页面内容的遮挡。

# Relevant Files
- `iep/index.html`
- `config.js`
- `advanced-settings.html`
- `index.html`
- `main.js`
- `test/iep-shell.test.js`
