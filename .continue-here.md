# Current State
- 任务：完成 SPEDMIS 原生 Electron AI 教师工作台 Phase 1，并接入 5 个现有智能体头像。
- 已完成：实现流式聊天、Provider、隐私确认、会话历史、额度控制、智能体切换和默认收起设置抽屉；5 张 PNG 已映射到稳定 agent code；AI 回归 9 套件 35 测试通过，生产构建成功。
- 产出物：ai-assistant.html、ai-assistant.css、ai-assistant.js、ai-preload.js、modules/ai-*.js、images/ai-agent-avatars/。

# Next Action
- 运行 `git status --short --branch`，确认未提交工作区和用户原有改动。

# Blockers
- 全量 Jest 的 4 个旧测试套件固定写入 `C:\Users\test`，当前账号无权限；AI 回归不受影响。
- 新增 AI 测试受 `.gitignore` 的 `test/` 规则忽略，后续提交前需显式处理。
- `PROJECT_CONTEXT.md` 含旧混合编码，后续只追加，不整体重写。

# Key Decisions
- 保持原生 HTML、CommonJS Electron Main/Preload、sql.js，不引入 Vue、Pinia、Element Plus 或额外原生依赖。
- 头像按稳定 agent code 显式映射到 `images/ai-agent-avatars`，加载失败回退文字头像。
- schema v2 一次性开启额度停止请求，用户之后手动关闭仍保留；服务设置使用按需抽屉。

# Relevant Files
- .continue-here.md
- PROJECT_CONTEXT.md
