# Current State
- 任务：为首页新增可配置的“综合测评领域”入口，并与“AI心理测验”互斥显示。
- 已完成：新增 `entry-module-manager` 持久化 `selectedModule`；新增 `iep-open-window` IPC 和 `openIepWindow()`；首页按配置动态渲染第六张卡片；高级设置新增单选入口配置；相关与全量 Jest 已通过。
- 产出物：`advanced-settings.html`、`index.html`、`main.js`、`preload.js`、`config.js`、`modules/entry-module-manager.js`、`modules/home-modules.js`、相关测试文件

# Next Action
- 运行 `npm start`，在高级设置切换为“综合测评领域”，验证首页只显示该入口且点击后打开 `iep/index.html`。

# Blockers
- None

# Key Decisions
- 使用单选 `selectedModule`（`psy` / `iep` / `none`）替代两个布尔开关：天然互斥，状态更稳定。
- 新增独立 `entry-module-manager`：入口显示配置不混入产品信息配置。
- `综合测评领域` 走独立窗口并直接加载 `iep/index.html`：复用现有 `AI心理测验` 打开模式，减少主进程改动。

# Relevant Files
- `advanced-settings.html`
- `index.html`
- `main.js`
- `preload.js`
- `config.js`
- `modules/entry-module-manager.js`
- `modules/home-modules.js`
