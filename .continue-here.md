# Current State
- 任务：诊断「AI 助手接入点 ID 重进后丢失」并提交推送本会话累积改动。
- 已完成：
  - 诊断（非代码 bug）：多接入点（endpoints_json）功能横跨渲染层 + 主进程模块，此前全未提交；运行中的主进程是 Phase 3 旧代码，忽略 endpoints 数组、只存单个 model。Ctrl+R 仅重载渲染层。落盘复现脚本证实工作区 DB 代码跨「关库→重开」持久化正常（DB/IPC/service/渲染读链路十处全核验通过）。
  - 修复路径：完全退出 app 重启 `npm start`（非 Ctrl+R）→ 主进程加载新代码 → Phase-5 迁移补 endpoints_json 列 → 之后接入点正常保存。旧主进程期间加的接入点未落盘、需重加一次。
  - 提交推送：`cefb692` feat(home) 首页视频背景 + AI FAB（5 文件，含 back.mp4 2.9MB）；`2db5923` feat(ai) provider 多接入点 + 系统维护可见性开关 + DeepSeek 下线（19 文件）。已 push origin/main。
- 产出物：commit cefb692、2db5923（= origin/main HEAD）

# Next Action
- 等待用户确认「重启 app 后多接入点持久化恢复正常」；当前无代码待写。

# Blockers
- None。代码已验证：11 个 JS `node --check` 过；Jest 33 套件过，4 个失败均为既有 `C:\Users\test` EPERM 基线（usage-stats/module-name-manager/permission-manager/entry-module-manager），与本次改动文件零交集。

# Key Decisions
- 多接入点用 ai_provider.endpoints_json 列（活动接入点 = model），schema 升 v5；CREATE TABLE + 迁移块都含该列（新鲜库 previousSchemaVersion=0 跳迁移）。
- 「智能体管理/知识技能/本月额度」默认隐藏，收口到系统维护的系统级开关（ai-feature-flags.json，FLAG_KEYS 集合 + merge 写入），AI 面板只读反映 state.features；per-owner knowledge_section_visible 偏好列保留为休眠字段（数据层未动）。
- 改动直接提交 main（项目惯例）。

# Relevant Files
- .continue-here.md
- PROJECT_CONTEXT.md
- modules/ai-database.js（endpoints_json：CREATE line 372 / 迁移 line 541-548 / mapProviderRow 167-194 / saveProvider 1339-1375）
- modules/ai-feature-flags-manager.js
