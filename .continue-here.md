# Current State
- 任务：验证并交付“激活后重启系统再次要求激活”的修复，重点看真实安装包/Electron 链路。
- 已完成：`main.js` 的 `checkActivationStatus()` 新增基于已保存 `deviceLedger` 的降级兼容判定；当当前机器码因硬件采集退化而变化时，只要保存的设备账本仍匹配，就认可已激活并迁移 `activation.json`；`test/activation-status.test.js` 新增对应回归用例并通过。
- 产出物：`main.js`、`test/activation-status.test.js`

# Next Action
- 运行 `npm run build`，生成包含激活兼容修复的新 Windows 安装包。

# Blockers
- None

# Key Decisions
- 激活判定优先仍走机器码匹配：只有机器码不匹配时才回退到已保存 `deviceLedger` 兼容判断。
- `deviceLedger` 兼容判断只比较当前能稳定拿到且非 `UNKNOWN_*` 的硬件字段：避免因为 `wmic`/`Get-CimInstance` 失效把同机误判成未激活。
- 不回滚上一轮首页高级设置入口改版和模块命名能力：本轮只补激活状态兼容逻辑。

# Relevant Files
- `main.js`
- `test/activation-status.test.js`
- `hardware.js`
- `modules/machine-code-manager.js`
- `package.json`
