# Current State
- 任务：修复「智能体聊天流式回复被 60s 总超时误中断」并更新交接文档、推送。
- 已完成：
  - 模型与额度面板视觉层级调整（commit `64e5677`，已推送 origin）：保存按钮改 teal 确认色；破坏操作（删除/清除密钥）克制化为 ghost 态、仅 hover 显红；选中接入点 teal 边框+teal-soft 底+teal 药丸徽章强强调；接入点列表改两行布局（ID 占满 ≈Base URL 宽、不再被截断，操作置第二行右对齐）；删除按钮文字→垃圾桶图标 + aria-label；标签"模型/接入点 ID"统一"接入大模型"；外发隐私确认独立 teal 卡片。
  - 流式超时修复（commit `17202a0`，待推送）：`streamChat` 原固定 60s 总超时会中断正在产出的流（用户实测：模型已在输出却被"请求超时 60 秒"打断）。改为空闲超时——`createRequestController` 增 `resetTimeout`，响应头到达及每段数据到达时重置计时，模型持续输出不再误杀，仅"两段数据间隔 >60s"才超时。`completeChat`/`testConnection` 总超时不变。`createAbortError` 据 `wasStreaming` 区分文案（连接/首段超时仍"请求超时（60 秒）"/ 产出停滞改"模型响应停滞，请稍后重试。"）。补 2 条回归测试，provider client 11/11 过。
- 产出物：`64e5677`（= origin/main）、`17202a0`（本地领先，待推送）

# Next Action
- 推送 `17202a0`（及本 handoff commit）到 origin/main。
- 等待用户**完全重启 app**（非 Ctrl+R，主进程模块改动）后，验证智能体长回复不再被总超时打断、且停滞时提示新文案。

# Blockers
- None。`modules/ai-provider-client.js` `node --check` 过；`npx jest ai-provider-client` 11/11 过。

# Key Decisions
- 流式超时语义从「总时长」改为「空闲间隔」（首 token + idle 超时），业界标准做法；一次性响应（completeChat）保留总超时。
- 超时文案按 `wasStreaming` 区分，避免产出停滞时误报"连接超时 60 秒"。
- 改动直接提交 main（项目惯例）。主进程模块（modules/ai-*.js）改动需整启 app 才生效，Ctrl+R 仅重载渲染层（曾导致接入点丢失误报，见 memory）。

# Relevant Files
- .continue-here.md
- PROJECT_CONTEXT.md
- modules/ai-provider-client.js（`createRequestController` / `resetTimeout` / `wasStreaming` / `createAbortError` / `streamChat` / `_requestJson`）
- test/ai-provider-client.test.js（新增"持续产出不超时"、"产出停滞触发新文案"两条）
