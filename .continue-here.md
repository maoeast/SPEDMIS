# Current State
- 任务：SPEDMIS AI 工作台 Phase 3「图片视觉与只读工具」已完成（未提交）。
- 已完成：
  - **3a 只读工具**：按 agent 开关启用（默认关）；启用工具的 agent 走非流式有界循环（MAX_TOOL_ROUNDS=4），其余保持流式不变。工具在 Main 执行。
    - 工具 1 `search_intervention_apps`：查 apps.json 干预应用目录，**剥离 应用路径/图标路径**，只返 name/domain/subcategory。
    - 工具 2 `query_usage_stats`：在 Main **直调** usage-stats 模块的 getUsageStats/getCategoryStats（不经其无门禁 IPC），只返聚合。
    - 安全：手写 JSON-schema 校验（additionalProperties 默认 false 拒未知键）+ 逐工具 Promise.race 超时(8s) + 结果 6000 字符裁剪 + `<tool_result>` 脱敏包裹 + 显式字段投影；新增 `ai_tool_call` 审计表。
  - **3b 图片附件**：全 rigor 校验（扩展名+MIME+魔数+尺寸 4096+单图 5MB+总量 4 张/10MB）+ sha256 + 生命周期(pending/attached/orphaned/deleted)。
    - 受控存储 `userData/ai-attachments/<convId>/`；DB 仅存相对路径/mime/size/sha256/width/height/status（**base64 不入库**）。
    - 视觉门控：provider 级 `supports_vision` 开关（默认关）；有图但 !supportsVision 时**本地拦截**（不建消息、不写文件、不请求）。
    - 多模态：当轮 user 消息组装为 `[{image_url},{text}]`（Main 内 base64，图先文后）。
    - 删会话时回收孤儿文件（refcount via listAttachmentPaths + cleanupOrphanedAttachments）。
- 产出物（新）：`modules/ai-tool-validator.js`、`modules/ai-tool-registry.js`、`modules/ai-attachment-store.js`、`test/ai-tool-validator.test.js`、`test/ai-tool-registry.test.js`、`test/ai-attachment-store.test.js`。
- 产出物（改）：`modules/ai-{database,service,ipc,provider-client}.js`、`ai-preload.js`、`main.js`、`ai-assistant.{html,css,js}`、5 个既有 ai 测试。
- 验证：AI 套件 89/89 通过（基线 63 → +26）；全量 Jest 仅 4 个 `C:\Users\test` 基线失败（无关）；`npm run build` 成功，新模块随包、无 `.md` 出货；`git diff --check` 干净。

# Next Action
- 等待用户指示：人工验收（§8：工具无法取得路径/执行、注入不能越白名单、图片可控存取无孤儿、关 supportsVision 本地拦截）或提交。当前改动均未提交。

# Blockers
- 全量 Jest 仍有 4 个基线失败套件（usage-stats/module-name-manager/permission-manager/entry-module-manager），固定写 `C:\Users\test`，当前账号无权限；与本阶段无关。
- 已知小限制（不阻塞）：图片历史回放（重建既往 user 消息的图片）暂缓——buildPromptMessages 不保留消息 id；附件 pending 超时 GC（cleanupStalePending）暂缓，删会话回收已覆盖主路径。
- `PROJECT_CONTEXT.md` 含旧混合编码，继续只追加不重写。

# Key Decisions
- 工具按 agent 开关启用（非全局），启用者走非流式循环、其余保持流式（零回归）。
- 图片校验全 rigor（§8 要求，超过 SIC-ADS 源实现的薄校验）。
- 视觉能力用 provider 级 `supports_vision` 开关（无模型注册表，不自动探测）。
- 工具结果不入 ai_message（role CHECK 限定 user/assistant），仅循环内存 + ai_tool_call 审计表。
- 工具结果回注前 `<tool_result>` 包裹 + 系统提示词约束（源无此防御）。

# Relevant Files
- .continue-here.md
- PROJECT_CONTEXT.md
- docs/planning/2026-07-23-spedmis-ai-agent-chat-migration-plan.md（§8 Phase 3 范围、§11 门禁）
