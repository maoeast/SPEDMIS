# Current State
- 任务：激活功能升级——①轮换 ACTIVATION_SECRET_KEY；②激活工具导出 .lis 激活文件；③激活页面导入 .lis 自动激活。
- 已完成：
  - **密钥轮换**：ACTIVATION_SECRET_KEY 换为 48 字符随机密钥（`4SznbOOn...`）；`embedded-secrets.js`（不入库）与 `tools/特殊教育多模态干预系统激活安全密钥.txt` 已同步；ENCRYPTION_KEY/IV 不动 → 已激活机器不受影响。旧激活码作废（新码用新密钥）。`tools/test-activation-generator.js` 17/17 通过（从 embedded 加载新密钥）。
  - **.lis 激活文件**：`modules/activation-lis.js` 单一实现（build/parse/文件名，v1 格式，容错 BOM/CRLF/注释/大小写）；IPC `parse-activation-lis`（config.js 通道 + main.js handler + preload.js API）。
  - **激活页导入**：activation.html 新增「导入激活文件(.lis)」→ 文件选择 → 主进程解析 → 机器码一致性校验 → 自动填入激活码并激活。
  - **工具导出**：CLI `--export-lis` / `--lis-output` + 交互模式生成后询问导出（单个/批量，批量导出到目录）；GUI 单个结果「导出激活文件(.lis)」按钮 + 批量结果每行「导出」按钮（server 新增 POST /api/lis-content）。
  - **测试与文档**：新增 `test/activation-lis.test.js` 15/15；全量 34/39 套件（5 个 EPERM 环境性失败为预存问题，与本次无关）。README/QUICKSTART/AGENTS/PROJECT_CONTEXT/系统参数技术文档 已更新。
  - **GUI v2.0**（activation-tool-gui.html 重写）：品牌蓝视觉 + 5 页签；新增「.lis 验证」页签（拖拽解析 + 校验有效性）；批量页新增「📦 导出全部 .lis（ZIP）」；server 新增 `/api/lis-parse`、`/api/lis-zip`；`tools/activation-zip.js` 无依赖 zip 生成器。测试 `test/activation-zip.test.js` 6/6 + server 端到端 5/5 通过；全量 35/40 套件。
  - **GUI v2.1（单文件版）**：按用户要求改为纯前端单文件（双击即用、零服务器）：内嵌 HMAC-SHA256（Web Crypto）/ .lis 解析 / 前端 ZIP / CSV；密钥内置 + localStorage 可改。`test/activation-frontend-core.test.js` 17/17（vm 提取内嵌脚本验证算法对等）。顺手修复 product-name-manager 版权断言。全量 37/41 套件（4 个 EPERM 预存问题）。
  - **tools 清理**：删除服务器模式整组（activation-tool-server.js / activation-zip.js / package.json / node_modules / test/activation-zip.test.js）+ gen-skill-catalog.cjs（一次性生成器）；README/QUICKSTART/模块注释同步改为单文件模式；保留激活核心工具与密钥文档。全量 36/40 套件（4 个 EPERM 预存问题）。

# Next Action
- 等待用户**完全重启 app**（主进程改动：main.js / preload.js / config.js / modules/activation-lis.js）后验证激活页导入 .lis 自动激活。
- 确认无问题后提交：激活升级相关改动（modules/activation-lis.js、config.js、main.js、preload.js、activation.html、tools/*、test/activation-lis.test.js、文档）→ 一个 feat 提交。
- 生产部署提醒：新密钥需随 `npm run build`（prebuild 注入 embedded-secrets.js）打包；若目标机使用环境变量方式，需在部署机设置新的 ACTIVATION_SECRET_KEY。

# Blockers
- None。主应用与工具侧密钥来源均为 embedded-secrets.js（已同步新密钥）；本地无 .env 覆盖。

# Key Decisions
- 只轮换 ACTIVATION_SECRET_KEY（用户确认）：激活状态校验只比对存储机器码（不重算 HMAC），已激活用户零影响；加密密钥不动避免已存 activation.json 无法解密。
- .lis 格式用文本 key=value（# 注释头 + machineCode/activationCode/issuedAt），解析容错最大化；构建/解析逻辑单点放在 modules/activation-lis.js，CLI/GUI server/主进程共用。
- 导入解析走主进程 IPC（业务逻辑在 modules/，渲染层不持有格式知识，逻辑可单测）。
- 激活页导入行为：机器码匹配时自动填入并提交激活（一键完成）；不匹配/格式错误给明确中文提示。

# Relevant Files
- modules/activation-lis.js（新，共享 .lis 格式）
- main.js（parse-activation-lis handler）、preload.js（electronAPI.parseActivationLis）、config.js（ipcChannels.parseActivationLis）
- activation.html（导入入口）
- tools/activation-tool-cli.js（--export-lis / 交互导出）、tools/activation-tool-server.js（/api/lis-content）、tools/activation-tool-gui.html（导出按钮）
- test/activation-lis.test.js（新，15 用例）
- embedded-secrets.js（新密钥）、tools/特殊教育多模态干预系统激活安全密钥.txt（新密钥）
